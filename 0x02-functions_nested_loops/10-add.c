#include "main.h"
/**
 *add - Entry point
 *Description: 'Adds two digits and returns the result'
 *@a: an integer to add
 *@b: an integer to add
 *Return: the value of a and b
 */
int add(int a, int b)
{
	int result;

	result = a + b;
	return (result);
}

